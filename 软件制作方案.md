# Markdown Viewer v2 软件制作方案

## 1. 目标理解

根据 `STYLE.md` 和 `项目功能与显示效果完整整理.md`，本软件的核心目标不是做一个简单的 Markdown 预览页面，而是制作一款面向 Windows 桌面的 Markdown 阅读与编辑工具。

它应同时满足三类需求：

- 阅读：默认进入渲染视图，提供安静、克制、适合长文阅读的纸面式界面。
- 编辑：支持源码精修、渲染区直接编辑、双栏对照校对，并能稳定同步。
- 文件工作流：支持打开、保存、另存为、拖拽导入、最近文件、未保存保护和本地图片路径解析。

最终产出应是一款可运行、可打包、可维护的桌面应用，而不仅是静态页面或原型。

## 2. 成品定义

### 2.1 一句话定位

Markdown Viewer v2 是一款文档优先的 Windows 桌面 Markdown 阅读与编辑器，默认强调舒适阅读，同时保留精确源码编辑和本地文件管理能力。

### 2.2 目标用户

- 需要长时间阅读技术文档、课程资料、项目说明的人。
- 需要校对 Markdown 排版效果的人。
- 需要在源码和渲染效果之间来回检查的人。
- 需要打开本地 Markdown 文件并保存修改的人。

### 2.3 核心体验标准

- 打开软件后，第一视觉焦点是文档标题和正文，不是工具栏。
- 默认视图是渲染视图，源码视图和双栏视图作为编辑增强能力存在。
- 所有文件操作都要保护未保存修改。
- 目录跳转和滚动联动必须稳定，不制造正文回弹或阅读打断。
- 视觉风格应严格靠近 `STYLE.md`：暖白、低饱和、弱控件、纸面感、长文友好。

## 3. 推荐技术路线

### 3.1 桌面壳

建议使用：

- Electron：负责 Windows 桌面窗口、系统菜单、文件对话框、窗口关闭拦截、拖拽安全设置和打包。
- Vite：负责前端开发服务器与构建。
- React + TypeScript：负责应用 UI、状态管理和组件组织。

理由：

- Electron 对本地文件读写、菜单、窗口控制和 Windows 打包支持成熟。
- Vite 启动快，适合分阶段开发。
- React + TypeScript 适合管理复杂状态，例如当前文档、视图模式、未保存状态、目录高亮、通知和双编辑器同步。

### 3.2 编辑与渲染

建议使用：

- CodeMirror 6：作为源码编辑器，提供行号、当前行、括号匹配、历史记录、软换行和语法高亮。
- Milkdown 或 ProseMirror 体系：作为渲染式可编辑 Markdown 表面。
- remark / rehype / unified 生态：用于标题提取、Markdown 结构处理、代码块增强和安全渲染辅助。

注意事项：

- 不建议一开始使用过重的一体化编辑器封装，否则后续包体积和细节控制会受限。
- 双向同步必须从第一天就设计“单一 Markdown 真相源”，避免两个编辑器各自维护内容后互相覆盖。

### 3.3 状态与数据

建议采用前端集中状态，不急于引入大型状态库。

核心状态包括：

- 当前 Markdown 内容。
- 已保存快照内容。
- 当前文件路径。
- 当前文件名。
- 当前视图模式：`render`、`source`、`split`。
- 未保存状态。
- 最近保存时间。
- 最近文件列表。
- 目录列表。
- 当前激活标题。
- 侧栏展开状态。
- 通知状态。
- 待确认的危险操作，例如打开新文件、关闭窗口、拖拽替换当前文档。

## 4. 建议项目结构

进入源码阶段后，建议目录如下：

```text
.
├─ electron/
│  ├─ main.ts
│  ├─ preload.ts
│  ├─ menu.ts
│  └─ file-service.ts
├─ src/
│  ├─ app/
│  │  ├─ App.tsx
│  │  ├─ app-state.ts
│  │  └─ app-types.ts
│  ├─ components/
│  │  ├─ AppShell.tsx
│  │  ├─ Sidebar.tsx
│  │  ├─ DocumentHeader.tsx
│  │  ├─ BottomBar.tsx
│  │  ├─ UnsavedChangesDialog.tsx
│  │  └─ DropOverlay.tsx
│  ├─ editor/
│  │  ├─ RenderEditor.tsx
│  │  ├─ SourceEditor.tsx
│  │  ├─ SplitEditor.tsx
│  │  └─ editor-sync.ts
│  ├─ markdown/
│  │  ├─ parse-headings.ts
│  │  ├─ slug.ts
│  │  ├─ markdown-stats.ts
│  │  ├─ image-resolver.ts
│  │  └─ code-blocks.ts
│  ├─ styles/
│  │  ├─ tokens.css
│  │  ├─ app.css
│  │  ├─ markdown.css
│  │  └─ responsive.css
│  └─ test-data/
│     └─ sample.md
├─ README.md
├─ AGENTS.md
├─ STYLE.md
├─ 项目功能与显示效果完整整理.md
└─ 软件制作方案.md
```

结构设计原则：

- `electron/` 只处理桌面系统能力。
- `src/components/` 只处理应用外壳和 UI 组件。
- `src/editor/` 专门处理渲染编辑器、源码编辑器和同步。
- `src/markdown/` 专门处理 Markdown 解析、目录、图片和统计。
- `src/styles/` 承接 `STYLE.md` 中的设计令牌和视觉规范。

## 5. 多 AGENTS 协作方案

项目功能较多，建议采用“主 Agent 统筹 + 专项 Agent 分工”的方式。每个 Agent 都应产出可检查的文件、代码或测试结果，避免只给建议不落地。

### 5.1 主控 Agent：项目架构与集成

职责：

- 拆分阶段目标。
- 维护任务优先级。
- 审查各 Agent 输出是否互相冲突。
- 负责最终合并、运行验证、提交 Git。
- 维护 `README.md`、`AGENTS.md` 和阶段记录。

验收标准：

- 每个阶段都有明确提交。
- 工作区不会长期处于混乱的半成品状态。
- 方案、代码、文档保持一致。

### 5.2 产品 Agent：需求与交互流程

职责：

- 把 `项目功能与显示效果完整整理.md` 拆成用户故事和验收标准。
- 维护功能优先级：MVP、v1、v1.5、v2。
- 梳理典型流程：阅读、精修、双栏校对、拖拽打开、未保存保护。

重点产出：

- `docs/需求拆解.md`
- `docs/验收清单.md`

### 5.3 UI Agent：视觉系统与响应式

职责：

- 将 `STYLE.md` 转换为 CSS token、组件规范和页面布局。
- 负责暖白纸面风格、字体、间距、颜色、按钮、侧栏、底部栏。
- 检查桌面、中等宽度、小屏窗口的显示效果。

重点产出：

- `src/styles/tokens.css`
- `src/styles/markdown.css`
- `src/styles/responsive.css`
- 视觉走查截图或记录。

### 5.4 Electron Agent：本地文件与桌面能力

职责：

- 实现打开、保存、另存为、关闭窗口拦截。
- 实现系统菜单、快捷键、最近文件菜单。
- 实现拖拽文件路径读取和安全设置。
- 维护 preload API，避免前端直接接触 Node 权限。

重点产出：

- `electron/main.ts`
- `electron/preload.ts`
- `electron/file-service.ts`
- 文件操作错误处理。

### 5.5 Editor Agent：编辑器与双向同步

职责：

- 实现源码视图。
- 实现渲染式可编辑视图。
- 实现双栏视图。
- 设计同步版本号、来源标记和防递归机制。

重点产出：

- `src/editor/SourceEditor.tsx`
- `src/editor/RenderEditor.tsx`
- `src/editor/SplitEditor.tsx`
- `src/editor/editor-sync.ts`

关键风险：

- 如果两个编辑器都直接写状态，容易出现循环更新、光标跳动和内容覆盖。
- 必须以“单一 Markdown 内容”为真相源，并记录每次更新来自哪个编辑器。

### 5.6 Markdown Agent：解析、目录、代码块、图片

职责：

- 提取标题并生成稳定锚点。
- 忽略代码块内部的伪标题。
- 清理标题中的链接、图片、强调符号。
- 处理重复标题。
- 实现词数、字符数、章节数统计。
- 实现本地相对图片路径解析和缺失占位。
- 实现代码块语言显示、复制、语言选择和预览切换。

重点产出：

- `src/markdown/parse-headings.ts`
- `src/markdown/slug.ts`
- `src/markdown/image-resolver.ts`
- `src/markdown/code-blocks.ts`
- 单元测试或样例验证。

### 5.7 QA Agent：测试、回归与体验验证

职责：

- 维护验收清单。
- 运行 lint、build、打包、手动冒烟测试。
- 检查关键回归：菜单无响应、关闭无响应、目录滚动抖动、双栏同步死循环。
- 记录已知问题和复现步骤。

重点产出：

- `docs/测试清单.md`
- `docs/问题记录.md`

### 5.8 Release Agent：打包、体积与发布

职责：

- 配置 Windows 安装包和便携版。
- 记录包体积和主要体积来源。
- 避免为了压缩体积牺牲核心功能。
- 维护版本号和发布说明。

重点产出：

- 打包配置。
- `docs/打包与发布.md`
- `CHANGELOG.md`

## 6. 分阶段执行路线

### 第 0 阶段：项目基线与开发约定

目标：

- 建立 Git 基线。
- 固化 README、AGENTS、制作方案。
- 明确技术路线、目录结构和阶段目标。

交付物：

- `README.md`
- `AGENTS.md`
- `.gitignore`
- `.gitattributes`
- `软件制作方案.md`

验收标准：

- Git 工作区干净。
- 项目说明文件能解释当前项目用途和下一步执行方式。

当前状态：

- 已完成 Git 初始化和基础说明文件。
- 本文件作为后续开发方案。

### 第 1 阶段：应用骨架

目标：

- 创建 Electron + Vite + React + TypeScript 项目。
- 启动后显示符合 `STYLE.md` 气质的基本界面。
- 建立左侧侧栏、顶部文档头、主内容区域、底部操作栏的空壳。

主要任务：

- 初始化 `package.json`、`tsconfig`、`vite`、`electron` 配置。
- 建立 `electron/main.ts` 和 `electron/preload.ts`。
- 建立 `src/App.tsx` 和基础 CSS token。
- 引入图标库，例如 Lucide。
- 添加示例 Markdown 内容。

验收标准：

- `npm run dev` 可启动桌面窗口。
- `npm run build` 可通过。
- 首屏不是空白。
- 视觉上已经是暖白纸面风格，而不是默认模板风格。

建议提交：

```text
搭建 Electron React 应用骨架
```

### 第 2 阶段：静态渲染阅读体验

目标：

- 实现 Markdown 渲染视图。
- 建立完整阅读排版：标题、段落、列表、引用、表格、行内代码、代码块、链接、图片、分割线。
- 默认进入渲染视图。

主要任务：

- 接入 Markdown 渲染管线。
- 将 `STYLE.md` 转换成 `markdown.css`。
- 控制正文最大行宽和长文阅读间距。
- 加入空状态和加载状态。

验收标准：

- 示例 Markdown 能完整渲染。
- 长段落不会铺满超宽屏。
- 表格和代码块不会撑破布局。
- 风格符合 `STYLE.md` 的暖白、低饱和、文档优先要求。

建议提交：

```text
实现 Markdown 渲染阅读视图
```

### 第 3 阶段：目录生成与侧栏导航

目标：

- 从 Markdown 自动生成目录。
- 支持目录点击跳转。
- 支持一二级目录编号、二级目录折叠和当前章节高亮。

主要任务：

- 实现标题解析。
- 实现稳定 slug。
- 过滤代码块内部标题。
- 实现目录组件。
- 实现点击目录后平滑滚动。

验收标准：

- 重复标题不会导致跳转冲突。
- 点击目录能定位到正确标题。
- 源码视图下点击目录会自动切回渲染视图再定位。
- 移动端目录可作为抽屉打开和关闭。

建议提交：

```text
实现自动目录与章节跳转
```

### 第 4 阶段：源码编辑视图

目标：

- 实现 CodeMirror 源码视图。
- 支持行号、当前行、高亮、软换行、Markdown 语法高亮和撤销重做。

主要任务：

- 接入 CodeMirror。
- 设计源码视图主题，使其保持暖白纸面风格。
- 实现视图切换控件：渲染、源码、双栏。
- 让源码编辑更新统一 Markdown 状态。

验收标准：

- 源码视图可编辑。
- 切换回渲染视图后内容同步。
- 大文档输入不卡顿到不可用。
- 源码视图不变成突兀的深色代码编辑器。

建议提交：

```text
实现源码编辑视图
```

### 第 5 阶段：文件打开、保存与未保存保护

目标：

- 完成本地 Markdown 文件工作流。
- 支持打开、保存、另存为和关闭保护。

主要任务：

- Electron 主进程实现文件对话框和读写。
- preload 暴露安全 API。
- 前端维护文件路径、文件名、保存快照和未保存状态。
- 实现未保存确认弹窗。
- 窗口标题显示未保存状态。

验收标准：

- 能打开 `.md` / `.markdown` 文件。
- 修改后显示未保存状态。
- 保存后状态变为已保存。
- 打开新文件或关闭窗口前会触发未保存保护。
- 取消危险操作后当前文档不变。

建议提交：

```text
实现本地文件打开保存与未保存保护
```

### 第 6 阶段：拖拽导入与最近文件

目标：

- 支持拖拽 Markdown 文件打开。
- 支持本次会话最近文件。

主要任务：

- 实现拖拽覆盖层。
- 过滤非 Markdown 文件。
- 处理拖拽文件路径读取失败。
- 主进程维护 session-only 最近文件。
- 侧栏和系统菜单同步最近文件列表。

验收标准：

- 拖入 Markdown 文件能打开。
- 拖入非 Markdown 文件有友好提示。
- 拖拽替换当前文档前会执行未保存保护。
- 最近文件重复打开时移动到顶部。
- 路径失效时能移除或提示。

建议提交：

```text
实现拖拽导入和会话最近文件
```

### 第 7 阶段：双栏视图与双向同步

目标：

- 实现源码和渲染同时可编辑。
- 两侧共享同一份 Markdown 内容。

主要任务：

- 实现 `SplitEditor`。
- 引入同步版本号和来源标记。
- 避免编辑器互相触发递归更新。
- 处理切换视图时的外部内容写入。
- 保持光标和滚动尽量稳定。

验收标准：

- 源码侧修改后渲染侧更新。
- 渲染侧修改后源码侧更新。
- 不出现无限更新、光标频繁跳回开头、内容丢失。
- 双栏在较窄窗口下自动改为上下排列。

当前状态：

- 已完成源码侧与渲染编辑侧双向同步，使用来源与版本快照防止递归回写。
- 已通过 Playwright 检查双向编辑、表格与任务列表内容保留、桌面双列、窄屏单列和控制台无错误。

建议提交：

```text
实现双栏编辑与内容同步
```

### 第 8 阶段：代码块工具条与图片路径

目标：

- 增强代码块显示与本地图片体验。

主要任务：

- 代码块显示语言。
- 复制代码。
- 语言选择面板和搜索。
- 代码预览开关。
- 相对图片基于当前文件目录解析。
- 缺失图片显示占位。

验收标准：

- 支持 Plain Text、Markdown、JSON、JavaScript、TypeScript、HTML、CSS、Bash、Python、SQL。
- 复制成功有通知。
- 语言选择浮层不遮挡正文关键内容。
- 相对图片在有文件路径时能正确显示。
- 缺失图片不会导致渲染崩溃。

建议提交：

```text
增强代码块工具条和图片路径解析
```

### 第 9 阶段：滚动联动、响应式与可访问性

目标：

- 完成目录随阅读滚动高亮。
- 完成桌面、中等宽度、小屏响应式。
- 补齐基本可访问性。

主要任务：

- 阅读滚动时更新当前章节。
- 当前目录项接近侧栏边界时轻微滚动到可见区域。
- 避免目录同步反向影响正文滚动。
- 实现侧栏抽屉。
- 补齐 aria、焦点状态、键盘访问和减少动态效果。

验收标准：

- 普通阅读滚动不会回弹。
- 当前章节高亮准确。
- 点击目录和平常滚动的逻辑分离。
- 小屏下按钮和文本不重叠。
- 主要控件可以键盘聚焦。

建议提交：

```text
完善目录滚动联动和响应式体验
```

### 第 10 阶段：测试、打包与发布

目标：

- 完成可发布版本。
- 建立持续回归检查习惯。

主要任务：

- 补充样例 Markdown 文档。
- 编写关键工具函数测试。
- 运行 lint、build、打包。
- 冒烟测试安装包或便携版。
- 记录包体积和已知限制。

验收标准：

- `npm run lint` 通过。
- `npm run build` 通过。
- `npm run dist` 或等效打包命令通过。
- 打包后的程序能打开、编辑、保存和关闭。
- README 记录启动、构建、打包命令。

建议提交：

```text
完成打包发布准备
```

## 7. 功能优先级

### MVP 必须完成

- Electron 桌面窗口。
- 暖白纸面式 UI 骨架。
- Markdown 渲染视图。
- 源码编辑视图。
- 渲染 / 源码 / 双栏视图切换。
- 打开、保存、另存为。
- 未保存修改保护。
- 自动目录与点击跳转。
- 基础响应式。

### v1 建议完成

- 双栏双向可编辑同步。
- 拖拽导入。
- session-only 最近文件。
- 目录滚动联动。
- 相对图片路径解析。
- 底部通知反馈。
- 代码块复制。

### v1.5 可以增强

- 代码块语言选择。
- 文档内搜索。
- 字号调节。
- 正文宽度调节。
- 阅读进度。
- 最近文件持久化选项。

### v2 之后再考虑

- 多标签文档。
- 文件夹或知识库模式。
- 导出 HTML / PDF。
- 深色主题。
- 插件化 Markdown 扩展。
- 结构化 front matter 编辑。

## 8. 关键风险与控制方法

### 8.1 双向同步风险

风险：

- 源码视图和渲染视图互相触发更新，造成死循环或内容覆盖。

控制方法：

- 只维护一个 canonical Markdown 字符串。
- 每次更新附带来源：`source`、`render`、`file`、`system`。
- 每次外部写入附带版本号。
- 编辑器收到自身来源的同步时不重复写入。

### 8.2 未保存保护风险

风险：

- 打开文件、拖拽文件、关闭窗口时丢失当前修改。

控制方法：

- 所有危险操作都走同一个 `confirmUnsavedChanges` 流程。
- 危险操作先保存为 pending action，用户确认后再执行。
- 取消操作必须保证当前文档完全不变。

### 8.3 目录滚动抖动风险

风险：

- 目录高亮同步反向触发正文滚动，导致阅读过程中回弹。

控制方法：

- 区分“用户普通滚动”和“点击目录导航滚动”。
- 普通滚动只更新高亮，不移动正文。
- 点击目录才允许平滑滚动正文。
- 侧栏自动滚动只作用于侧栏自身。

### 8.4 本地路径与图片风险

风险：

- Electron 安全限制、拖拽路径读取、相对图片解析和缺失图片处理容易出错。

控制方法：

- 所有本地文件能力都通过 preload 暴露最小 API。
- 相对图片必须依赖当前 Markdown 文件所在目录。
- 临时文档没有基准路径时显示缺失占位。
- 图片加载失败不影响整篇文档渲染。

### 8.5 包体积风险

风险：

- Markdown 编辑器和语法高亮库可能让应用体积迅速膨胀。

控制方法：

- 源码编辑器懒加载。
- 代码高亮语言白名单化。
- 避免引入大而全的编辑器封装。
- 每次打包后记录 `dist`、安装包和解压目录大小。

## 9. 验收清单

开发过程中应持续检查：

- 启动后默认是渲染视图。
- 顶部标题能从一级标题或文件名推导。
- 词数、字符数、章节数能更新。
- 打开文件、保存、另存为都能正常工作。
- 未保存状态能正确显示。
- 关闭窗口前会保护未保存内容。
- 目录能过滤代码块内的伪标题。
- 重复标题跳转不冲突。
- 点击目录不会造成错误视图状态。
- 普通阅读滚动不会被目录同步打断。
- 源码视图编辑后渲染视图同步。
- 双栏两侧编辑不会造成死循环。
- 拖拽非 Markdown 文件有提示。
- 相对图片能按文件目录解析。
- 缺失图片有占位。
- 代码块复制有反馈。
- 窄屏下侧栏能作为抽屉打开。
- 小屏下按钮和文字不重叠。
- 键盘焦点可见。
- 打包后的应用能完成打开、编辑、保存、关闭冒烟流程。

## 10. Git 执行节奏

建议每个阶段至少一个提交，不把大量不相关改动压在一起。

推荐命令：

```powershell
git status --short --branch
git add .
git commit -m "阶段说明"
git log --oneline --decorate -5
```

推荐提交粒度：

- 一个架构阶段一个提交。
- 一个功能闭环一个提交。
- 一个修复一个提交。
- 文档更新可以跟随对应功能提交，必要时单独提交。

每次提交前应确认：

- README 是否需要更新。
- AGENTS 是否需要更新。
- 当前阶段是否有验收结果。
- 是否存在误加入的构建产物或临时文件。

## 11. 推荐的第一轮实际执行

下一轮不要直接冲复杂功能，建议先完成以下闭环：

1. 初始化 Electron + React + TypeScript 项目骨架。
2. 建立 `tokens.css`，把 `STYLE.md` 的颜色、字体、间距落成变量。
3. 做出静态应用外壳：侧栏、文档头、主内容区、底部栏。
4. 放入一份示例 Markdown，完成只读渲染。
5. 跑通 `npm run dev` 和 `npm run build`。
6. 提交为“搭建 Electron React 应用骨架”。

这个顺序的好处是先得到一个可运行、可查看、可继续扩展的基础界面。之后再加入文件读写、源码编辑器、双栏同步等复杂功能时，每一步都有稳定基线可以回退。
