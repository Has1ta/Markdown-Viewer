export const sampleMarkdown = `# Markdown Viewer v2 示例文档

这是一份用于应用骨架阶段的内置文档。它的作用是让桌面窗口启动后立即呈现真实阅读内容，而不是只看到一张空白模板。

## 阅读优先

Markdown Viewer v2 的默认模式是渲染视图。侧栏、工具条和状态信息都应该服务于正文，不抢走文档本身的注意力。

> 一个好的阅读工具应该让人感觉内容自然铺开，控件在需要时才出现。

## 编辑入口

当前阶段先提供轻量源码输入区，后续会替换为 CodeMirror 6，并继续保留统一 Markdown 状态。

- 渲染视图用于阅读和校对。
- 源码视图用于精修 Markdown。
- 双栏视图用于对照检查。

## 表格样式

| 能力 | 当前状态 | 后续阶段 |
|---|---|---|
| Electron 窗口 | 已纳入骨架 | 加入文件菜单 |
| React UI | 已纳入骨架 | 拆分更多组件 |
| Markdown 渲染 | 初版示例 | 增强代码块与图片 |

## 代码块

\`\`\`ts
type ViewMode = "render" | "source" | "split";
\`\`\`

## 下一步

下一阶段会继续补齐文件打开、保存、目录跳转和更正式的编辑器能力。`;
