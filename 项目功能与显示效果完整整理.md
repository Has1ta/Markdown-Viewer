# Markdown Viewer 功能与显示效果说明

## 1. 产品定位

Markdown Viewer 是一款文档优先的 Markdown 桌面阅读与编辑工具。它的核心体验不是把 Markdown 放进普通文本框，而是让用户在同一个界面里完成阅读、渲染校对、源码精修、目录导航、文件保存和长文档管理。

整体体验目标：

- 默认以“阅读”作为第一体验。
- 编辑能力服务于文档校对，而不是抢占视觉焦点。
- 适合长时间阅读技术文档、课程资料、项目说明和知识库内容。
- 支持精确源码编辑，也支持接近成品形态的渲染式编辑。
- 保持单文档工作流，降低复杂度。
- 面向 Windows 桌面使用场景，强调本地文件打开、保存和拖拽导入。

## 2. 主界面整体结构

主界面由两个核心区域组成：

- 左侧侧栏：承载文档导航、文档摘要、目录、最近文件和提示信息。
- 右侧主区域：承载文档标题、操作区、编辑/阅读视图和底部操作栏。

桌面宽屏下，左侧侧栏常驻，右侧主区域占据主要空间。整体布局居中，最大宽度受限，避免内容在宽屏上拉得过散。

窄屏下，左侧侧栏会变成抽屉式面板，主区域改为单列布局。这样既保留目录导航能力，又不挤压正文。

## 3. 视觉风格

整体视觉采用暖白纸面风格。

主要特征：

- 页面背景是浅米白，不使用纯白背景。
- 内容面板接近纸张颜色，边框为浅暖灰。
- 阴影非常轻，只用于区分层级，不制造强悬浮感。
- 强调色为低饱和陶土橙。
- 成功状态使用柔和灰绿色。
- 文本颜色偏暖灰，不使用纯黑。
- 控件弱化，正文和标题是第一视觉焦点。

这种风格让界面更像一份可阅读、可编辑的出版型文档，而不是传统后台面板或深色代码编辑器。

## 4. 字体与阅读感

界面默认使用内置字体体系，标题、正文、按钮、目录、代码区域保持统一字形。

显示效果：

- 大标题有明显文档封面感。
- 正文字号适合长时间阅读。
- 行高较舒展，段落之间有清晰呼吸感。
- 代码、路径、快捷键等信息使用等宽风格。
- 中文、英文、数字和代码混排时视觉比较统一。

正文阅读区会限制最大行宽，避免宽屏下单行过长导致阅读疲劳。

## 5. 顶部文档头

顶部文档头是主区域的第一个大面板，用来展示当前文档身份和主要操作。

包含内容：

- 侧栏开关按钮。
- 小标题标识：`Markdown 阅读器`。
- 当前文章标题。
- 文档元信息。
- 打开按钮。
- 视图切换控件。

文档元信息包括：

- 当前是临时文档还是已关联本地文件。
- 最近保存时间。
- 词数。
- 字符数。

标题来源规则：

- 如果文档中存在一级标题，顶部标题优先使用该一级标题。
- 如果没有一级标题，则使用文件名。
- 如果文件名也不可用，则使用默认临时文档名称。

显示效果：

- 标题字号大、字重克制。
- 元信息以小号文字排在标题下方。
- 各元信息之间用圆点分隔。
- “打开”按钮是次级文档操作，视觉上比视图切换更轻。
- 视图切换是 segmented control，当前选中项使用浅色背景突出。

## 6. 视图模式

应用提供三种视图模式。

### 6.1 渲染视图

渲染视图是默认模式。

适合场景：

- 阅读长文档。
- 检查标题层级。
- 校对段落、列表、引用、表格、图片和代码块的最终效果。
- 进行偏可视化的 Markdown 编辑。

显示效果：

- 只显示渲染后的文档面板。
- 面板顶部显示“渲染视图”标签。
- 标签下方说明该视图适合流畅阅读、结构调整和视觉校对。
- 正文排版接近最终阅读效果。
- 用户可以直接在渲染内容中编辑。

### 6.2 源码视图

源码视图用于精确编辑 Markdown 原文。

适合场景：

- 调整空行。
- 精确控制缩进。
- 编辑复杂表格。
- 检查 Markdown 语法。
- 编辑代码块或 front matter。

显示效果：

- 只显示源码编辑区域。
- 左侧有行号。
- 当前行有轻微背景高亮。
- Markdown 符号和文本有低饱和语法颜色。
- 编辑区自动换行。
- 整体仍保持暖白纸面风格，而不是切换成深色编辑器。

### 6.3 双栏视图

双栏视图同时展示源码和渲染结果。

适合场景：

- 一边写 Markdown，一边看渲染效果。
- 校对表格、列表、代码块。
- 检查图片、链接、引用是否正确显示。
- 进行源码和视觉之间的来回核对。

显示效果：

- 桌面宽屏下，左侧为源码视图，右侧为渲染视图。
- 两个面板都有独立标题栏。
- 两边都可以编辑。
- 源码变化会同步到渲染视图。
- 渲染视图变化也会同步回源码视图。
- 较窄桌面宽度下，双栏会退回上下单列排列，避免内容被横向裁切。

## 7. 文档状态

应用围绕一份当前文档工作。

文档状态包括：

- 文件路径。
- 文件名。
- Markdown 内容。
- 是否存在未保存修改。
- 最近保存时间。

状态显示位置：

- 左侧摘要卡片。
- 顶部文档头。
- 窗口标题。
- 底部通知栏。

保存状态：

- 已保存：显示灰绿色状态芯片。
- 未保存：显示浅陶土橙状态芯片。
- 窗口标题会用特殊标记提示未保存状态。

## 8. 打开文件

打开功能支持：

- 点击顶部“打开”按钮。
- 使用系统菜单打开。
- 使用快捷键打开。
- 从最近文件打开。
- 拖拽 Markdown 文件到窗口打开。

打开文件前，如果当前文档有未保存修改，会先触发未保存保护。

文件限制：

- 只面向 Markdown 文件。
- 默认按 UTF-8 文本读取。
- 不符合要求时会显示错误提示。

打开成功后会更新：

- 当前文档内容。
- 文件名。
- 文件路径。
- 最近保存时间。
- 保存状态。
- 最近文件列表。
- 目录。
- 顶部标题和统计信息。

## 9. 保存与另存为

保存功能包括：

- 保存当前文件。
- 另存为新文件。
- 没有文件路径时，保存会转入另存为流程。
- 保存成功后文档变为已保存状态。
- 保存成功后更新最近保存时间。
- 保存成功后更新最近文件列表。

底部操作栏提供：

- 保存。
- 另存为。

另存为按钮是主按钮，使用陶土橙填充。

保存中按钮会进入禁用或等待状态，避免重复操作。

## 10. 未保存修改保护

在以下场景会检查未保存修改：

- 打开另一个文件。
- 从最近文件打开文件。
- 拖拽文件替换当前文档。
- 关闭窗口。

如果存在未保存修改，用户可以选择：

- 保存后继续。
- 不保存并继续。
- 取消当前操作。

取消后：

- 当前文档保持原样。
- 操作不会继续执行。
- 底部会显示提示，说明已保留未保存修改。

这可以防止用户在切换文档或关闭应用时误丢内容。

## 11. 拖拽导入

拖拽导入的交互过程：

1. 用户把文件拖进窗口。
2. 界面出现半透明覆盖层。
3. 中央显示提示卡片。
4. 松手后读取拖入文件。
5. 如果是 Markdown 文件，则尝试打开。
6. 如果当前文档未保存，先进入未保存保护。

覆盖层显示内容：

- 主文案：拖入 Markdown 文件即可打开。
- 副文案：替换当前文档之前会先执行未保存修改保护。

错误场景：

- 无法读取拖入文件路径。
- 拖入文件不是 Markdown 文件。
- 文件读取失败。

这些情况会通过底部通知提示。

## 12. 最近文件

最近文件只保留在当前应用会话内，不做持久化。

显示位置：

- 左侧侧栏“最近文件”区域。
- 系统菜单的最近文件子菜单。

每条最近文件显示：

- 文件名。
- 完整路径。

行为：

- 打开文件后加入最近文件。
- 保存和另存为后也会更新最近文件。
- 同一路径重复打开时会移动到列表顶部。
- 最近文件数量有限制，超过后保留最新若干项。
- 如果最近文件路径失效，会自动从列表移除。

## 13. 左侧侧栏

左侧侧栏是文档上下文中心。

包含：

- 文档导航品牌区。
- 文档摘要卡片。
- 自动目录。
- 最近文件。
- 使用提示。
- 收起侧栏按钮。

显示效果：

- 侧栏是圆角纸面容器。
- 桌面端固定在视口内。
- 内容超出时侧栏内部滚动。
- 底部收起按钮使用胶囊形状。

桌面端交互：

- 点击顶部“侧栏”按钮可以展开或收起整条侧栏。
- 点击侧栏底部按钮可以收起侧栏。
- 收起后主区域占满可用宽度。

移动端交互：

- 侧栏以抽屉形式从左侧出现。
- 背后显示半透明遮罩。
- 点击遮罩可关闭抽屉。
- 点击目录项后会自动关闭抽屉。

## 14. 文档摘要卡片

摘要卡片展示当前文档概况。

包含：

- 保存状态。
- 当前视图模式。
- 文件名。
- 文件路径或临时文档提示。
- 章节数量。
- 最近保存时间。
- 当前文章标题。
- 词数。
- 字符数。

显示效果：

- 状态芯片位于左上。
- 当前视图位于右上。
- 文件名加粗显示。
- 路径使用小号等宽文字。
- 统计信息使用低对比小号文字。

## 15. 自动目录

目录由 Markdown 标题自动生成。

目录规则：

- 优先把文档一级标题作为文章标题。
- 目录主要展示文档的主要章节和一级子章节。
- 更深层级标题会归属到最近的可见父级章节。
- 标题中的链接、图片、强调符号会被清理后作为目录文本。
- 代码块内部看起来像标题的文本不会进入目录。
- 重复标题会拥有稳定的内部锚点，避免跳转冲突。

目录显示：

- 一级目录项有数字编号。
- 二级目录项有小编号，如 `1.1`、`1.2`。
- 二级目录相对一级目录缩进。
- 二级目录左侧有竖向引导线。
- 当前章节使用浅色背景高亮。

目录折叠：

- 目录区域右上角按钮只折叠或展开二级目录。
- 折叠后保留一级目录。
- 这与整条侧栏的收起功能分开，避免交互含义混淆。

## 16. 目录跳转与滚动联动

目录支持点击跳转。

点击目录后：

- 当前目录项立即进入激活状态。
- 渲染区平滑滚动到目标标题。
- 移动端会关闭目录抽屉。
- 如果当前在源码视图，会自动切回渲染视图完成定位。

普通阅读滚动时：

- 当前章节会随滚动自动更新。
- 左侧目录会同步高亮当前章节。
- 当高亮项快移出侧栏可视区域时，侧栏会轻微滚动，让高亮项重新露出。
- 普通滚动只更新当前章节，不主动改变正文滚动位置。

滚动体验目标：

- 不在阅读过程中造成回弹。
- 不在代码块和表格附近造成抖动。
- 不因为目录同步打断用户阅读。

## 17. Markdown 内容显示

### 17.1 标题

标题显示层级清晰：

- H1 字号最大，用于文档主标题。
- H2 用于主要章节。
- H3 用于章节内小节。
- 标题字重克制，不使用过粗字重。
- 标题之间通过字号、留白和层级区分，而不是高饱和颜色。

### 17.2 正文段落

正文显示特点：

- 字号适中。
- 行高较大。
- 段落之间有稳定间距。
- 行宽受控。
- 适合长时间阅读。

### 17.3 列表

支持：

- 无序列表。
- 有序列表。
- 多级列表。
- 任务列表。

显示特点：

- 列表缩进清晰。
- 列表项之间有轻微间距。
- 任务列表勾选框使用低饱和绿色强调。

### 17.4 引用块

引用块采用完整浅色容器，而不是单独一条左边框。

显示特点：

- 浅色背景。
- 细边框。
- 圆角。
- 内边距较充足。
- 文本颜色比正文略弱。

这种效果更像文档中的提示卡片，适合课程笔记和技术说明。

### 17.5 表格

表格显示特点：

- 占满正文可用宽度。
- 外层有圆角。
- 表头使用浅色背景。
- 单元格有细边框。
- 单元格内边距较舒适。
- 表头字重略高。
- 表格整体克制，不使用重色块。

窄屏下，表格保持可读优先，避免撑破布局。

### 17.6 行内代码

行内代码显示特点：

- 浅米灰背景。
- 细边框。
- 小圆角。
- 等宽字体。
- 陶土棕色文字。

它在正文中明显可见，但不会像高亮标签一样过度抢眼。

### 17.7 代码块

普通代码块显示特点：

- 浅米灰背景。
- 细边框。
- 圆角。
- 支持横向滚动。
- 使用等宽字体。

增强代码块还带有工具条，详见代码块工具条部分。

### 17.8 链接

链接显示特点：

- 使用陶土橙。
- 默认不加下划线。
- 悬停时出现下划线。
- 下划线有轻微偏移，避免贴住文字。

### 17.9 图片

图片显示特点：

- 最大宽度不超过内容区域。
- 圆角。
- 浅边框。
- 独立块级显示。

相对图片路径会按当前文档所在位置解析。

如果图片缺失：

- 显示内置缺失图片占位。
- 图片边框变成虚线。
- 使用强调色提示异常。
- 鼠标悬停可看到缺失路径提示。

### 17.10 分割线

分割线显示为浅色细线，上下留白明显，用于分隔大块内容。

## 18. 代码块工具条

渲染视图中的增强代码块包含顶部工具条。

工具条能力：

- 显示当前代码语言。
- 打开语言选择面板。
- 搜索语言。
- 复制代码。
- 切换代码预览。

语言选择面板：

- 以浮层形式出现。
- 有圆角、边框和轻微阴影。
- 顶部是搜索框。
- 下方是语言列表。
- 没有搜索结果时显示空状态。

按钮显示：

- 高度统一。
- 圆角统一。
- 背景是浅暖白。
- hover 时背景轻微变深。
- focus 时出现柔和焦点环。

代码块选中状态：

- 外边框变为更明显的强调色混合边框。
- 外层出现柔和光晕。
- 让用户知道当前正在编辑该代码块。

支持的代码语言包括：

- Plain Text
- Markdown
- JSON
- JavaScript
- TypeScript
- HTML
- CSS
- Bash / Shell
- Python
- SQL

## 19. 源码编辑体验

源码编辑区适合精修 Markdown。

编辑体验：

- 显示行号。
- 当前行高亮。
- 当前行 gutter 高亮。
- 支持括号匹配。
- 支持 Tab 缩进。
- 支持历史撤销重做。
- 支持自动换行。
- 支持 Markdown 语法高亮。
- 选区颜色与整体主题保持一致。

视觉特点：

- 背景保持暖白。
- gutter 使用略深的浅米灰。
- 语法颜色低饱和。
- 光标使用强调色。
- 搜索匹配、括号匹配等状态也使用柔和背景。

## 20. 双向同步

渲染视图和源码视图共享同一份 Markdown 内容。

同步规则：

- 从源码视图输入后，渲染视图更新。
- 从渲染视图编辑后，源码视图更新。
- 打开文件后，两种编辑器都同步到新内容。
- 保存后，当前内容作为新的已保存快照。
- 用户编辑后，如果内容不同于已保存快照，则标记为未保存。

同步体验目标：

- 用户可以自由切换视图，不丢内容。
- 双栏中两侧可以实时核对。
- 外部同步不会造成明显闪烁。
- 避免编辑器把自身同步结果再次误判为用户输入。

## 21. 图片路径处理体验

图片路径处理对用户呈现为：

- 网络图片正常显示。
- 本地绝对路径图片正常显示。
- 本地相对路径图片根据当前 Markdown 文件位置显示。
- 临时文档中的相对图片，如果没有基准路径，会显示缺失占位。
- 图片不存在时不会让界面崩溃，而是显示可理解的占位图。

这对本地知识库、课程材料和项目文档很重要，因为这些文档经常使用相对图片目录。

## 22. 底部操作栏

底部操作栏是辅助操作区。

包含：

- 返回顶部。
- 保存。
- 另存为。
- 状态通知。

显示特点：

- 圆角纸面容器。
- 左侧是操作按钮组。
- 右侧是通知文本。
- “另存为”是主按钮。
- 其他按钮保持轻量。

返回顶部：

- 点击后页面平滑滚动到顶部。
- 属于显式导航滚动。

保存和另存为：

- 操作期间会进入等待状态。
- 完成后通过通知反馈结果。

## 23. 通知反馈

通知显示在底部操作栏右侧。

通知类型：

- 成功：保存成功、打开成功。
- 错误：打开失败、保存失败、拖拽失败。
- 信息：取消操作、准备就绪。
- 静默提示：没有主动通知时显示默认使用建议。

行为：

- 通知会自动消失。
- 新通知会覆盖旧通知。
- 不使用弹窗打断用户，除非涉及未保存修改保护。

显示效果：

- 成功通知使用柔和绿色。
- 错误通知使用低饱和红棕色。
- 信息通知使用次级文本色。

## 24. 移动端与窄屏响应式

### 24.1 中等宽度

当窗口变窄时：

- 页面左右边距减小。
- 侧栏变窄。
- 主内容内边距减小。
- 双栏视图从左右并排变成上下排列。

这样可以避免源码和渲染内容被横向裁切。

### 24.2 小屏宽度

小屏下：

- 主界面变为单列。
- 侧栏默认隐藏。
- 点击“侧栏”按钮打开目录抽屉。
- 抽屉从左侧覆盖出来。
- 背景出现半透明遮罩。
- 顶部操作、视图切换和底部按钮改为纵向排列。
- 按钮宽度变为 100%。
- 正文内边距缩小。
- 代码块工具条可换行。

抽屉显示：

- 宽度不超过屏幕大部分区域。
- 高度贴近视口。
- 内部可滚动。
- 圆角和阴影比桌面侧栏更明显。

## 25. 加载与空状态

加载状态：

- 渲染视图和源码视图都支持懒加载。
- 加载时显示“正在加载渲染视图...”或“正在加载源码编辑器...”。

目录空状态：

- 如果文档没有可导航标题，目录区域显示空提示。

最近文件空状态：

- 如果本次启动后还没有打开过文件，显示“本次启动后还没有最近文件记录”。

这些空状态都使用低对比文本，不会打扰阅读。

## 26. 可访问性与交互细节

可访问性体验：

- 主要按钮和目录按钮可通过键盘聚焦。
- 焦点状态有清晰轮廓。
- 视图切换使用 tablist 语义。
- 侧栏抽屉有展开状态。
- 目录折叠按钮有展开状态。
- 通知区域使用状态语义。
- 支持系统减少动态效果设置。

交互动效：

- hover 使用背景、文字、边框颜色变化。
- 不使用夸张缩放。
- 展开折叠使用短时长缓动。
- 平滑滚动只用于明确导航动作。
- 用户普通阅读滚动不会被额外定位打断。

## 27. 典型使用流程

### 27.1 阅读已有文档

1. 打开 Markdown 文件。
2. 默认进入渲染视图。
3. 查看顶部标题和摘要。
4. 使用左侧目录跳转章节。
5. 滚动阅读时目录自动高亮当前章节。

### 27.2 精修 Markdown 原文

1. 切换到源码视图。
2. 根据行号定位内容。
3. 修改空行、缩进、表格或代码块。
4. 保存文件。

### 27.3 对照校对

1. 切换到双栏视图。
2. 左侧修改源码。
3. 右侧查看渲染结果。
4. 如果渲染区直接修改，也会同步回源码。
5. 校对完成后保存。

### 27.4 拖拽打开

1. 将 Markdown 文件拖入窗口。
2. 看到拖拽覆盖层提示。
3. 松手打开文件。
4. 如果当前文档未保存，先处理保存确认。

## 28. 当前体验边界

当前项目已经覆盖 Markdown 阅读器与编辑器核心功能，但仍有一些产品边界：

- 最近文件只在当前会话保留。
- 文件读写默认面向 UTF-8 Markdown。
- 当前是单文档工作流，不是多标签编辑器。
- 相对图片需要依赖当前文档路径，临时文档没有路径时无法解析相对图片。
- 移动端响应式是可用兜底，主要使用场景仍是桌面窗口。
- 词数统计更适合英文空格分隔文本，对中文不是精确分词。

## 29. 后续可改进方向

### 29.1 功能增强

- 增加新建文档入口。
- 增加文档内搜索。
- 增加导出 HTML 或 PDF。
- 增加复制当前章节链接。
- 增加最近文件持久化选项。
- 增加打开文件夹或知识库模式。

### 29.2 阅读体验

- 增加阅读进度显示。
- 增加字号调节。
- 增加正文宽度调节。
- 增加主题切换。
- 增加只读阅读模式。

### 29.3 编辑体验

- 增强表格编辑。
- 增加 front matter 折叠或结构化编辑。
- 增加源码与渲染滚动同步开关。
- 增加代码块语言的当前选中标识。
- 优化中文词数统计。

### 29.4 视觉与动效

- 为缺失图片占位图增加中文提示。
- 继续优化窄屏下长文档的留白比例。
- 增加深色主题，但保持低饱和、克制的文档气质。
- 优化目录高亮过渡，让章节切换更自然。

## 30. 总结

Markdown Viewer 当前已经形成完整的桌面 Markdown 阅读与编辑体验：它支持打开、保存、另存为、拖拽导入、最近文件、未保存保护、渲染视图、源码视图、双栏视图、自动目录、滚动高亮、相对图片解析、代码块工具条、响应式侧栏和底部通知反馈。

它的主要优势是把“长文档阅读”和“Markdown 精修”放在同一个安静的纸面式界面中，让用户既能舒服阅读，也能随时进入精确编辑。
